

## TensorCash (GPU)

官网：https://tensorcash.org/

X：https://x.com/Tensorcash

源码：https://git.tensorcash.org/tensorcash

TG：https://t.me/TensorCash_org

DC：https://discord.gg/xR6CjKm4EY

如何挖掘 TensorCash 详细教程：https://tensorcash.org/blog/how-to-mine/





矿池：https://tensorcash.luckypool.io/

矿池：https://tensorcash.boblabs.eu/

矿池：https://pool.ariabrain.com/tsc.html



OTC：https://tensorcash.xyz/zh-cn

交易所：https://cexius.com/trade/TSC-USDT



> 2026.07.24
>
>   查询时主网高度约 16653。当前 TensorCash 区块奖励是 92.664 TSC/块。
>
>   按官网/代码的目标出块时间 10 分钟 算：
>
>   144 块/天 × 92.664 TSC = 13,343.616 TSC/天
>
>   但按最近 24 小时实际出块数估算，约 600 块/天：
>
>   600 × 92.664 = 55,598.4 TSC/天
>
>   所以结论：理论日排放约 13,343.616 TSC；按当前实际链速，最近 24 小时约 55,598 TSC。 这里说的是新增区块补贴，不含手续费。





## TensorCash 钱包基本操作

https://github.com/tensorcash/tensorcash/releases

本文档用于快速说明 TensorCash 钱包的常见操作：创建钱包、生成收款地址、查询余额、备份、转账，以及用于矿池挖矿时需要注意的事项。



#### 运行节点

```sh
## 运行节点
docker rm -f tensorcash-wallet
docker run --platform linux/amd64 -d --name tensorcash-wallet \
    -e GUI_MODE=false \
    -e NODE_START_FLAGS='--desktop' \
    -e VALIDATION_API_MODE=desktop \
    -e WALLET_ADDRESS='' \
    -e CHECK_INTERVAL=60 \
    -e MODEL_API_KEY='' \
    -e VANITY_ONION_ENABLED=false \
    -e ONION_CLAIM_ENABLED=false \
    -e MINING_AUTOSTART=false \
    -e RPC_USER=tensorcashrpc \
    -e RPC_PASS=tensorcashrpc-local-only \
    -e RPC_PORT=39243 \
    -e COOKIE_FILE=/data/tensor/.cookie \
    -v tensorcash-data:/data \
    --entrypoint sh \
    ghcr.io/tensorcash/core-node:v1.0.5 \
    -lc "printf '%s\n' 'chain=tensor' 'server=1' 'listen=1' '' '[tensor]' 'rpcbind=127.0.0.1' 'rpcport=39243' 'rpcallowip=127.0.0.1'
    'rpcuser=tensorcashrpc' 'rpcpassword=tensorcashrpc-local-only' > /data/bitcoin.conf; exec /usr/bin/supervisord -c /etc/supervisor/conf.d/
    supervisord.conf"

## 检查同步状态
docker exec tensorcash-wallet \
bitcoin-cli -datadir=/data -conf=/data/bitcoin.conf getblockchaininfo
```



#### 1. 钱包类型

TensorCash Core 钱包基于 Bitcoin Core 体系，常见使用方式有两种：

- 图形钱包：`bitcoin-qt`
- 命令行钱包：`bitcoin-cli`

矿池挖矿需要使用主网地址，地址通常以：

```text
tc1q...
```

开头。不要使用测试网或 regtest 地址。

#### 2. 创建钱包

如果已经运行 TensorCash Core 节点，可以用 CLI 创建一个钱包：

```bash
bitcoin-cli createwallet miner
```

这里的 `miner` 是钱包名称，可以按需替换。

如果你使用 Docker 容器运行钱包，命令通常类似：

```bash
docker exec -it tensorcash-wallet bitcoin-cli -datadir=/data createwallet miner
```

#### 3. 生成收款地址

生成一个新的主网收款地址：

```bash
bitcoin-cli -rpcwallet=miner getnewaddress "" bech32
```

Docker 容器方式：

```bash
docker exec -it tensorcash-wallet bitcoin-cli -datadir=/data -rpcwallet=miner getnewaddress "" bech32
```

返回结果示例：

```text
tc1qxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

这个地址可以用于矿池参数：

```bash
--wallet tc1qxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

#### 4. 查询余额

查看钱包余额：

```bash
bitcoin-cli -rpcwallet=miner getbalance
```

Docker 容器方式：

```bash
docker exec -it tensorcash-wallet bitcoin-cli -datadir=/data -rpcwallet=miner getbalance
```

注意：刚收到的矿池付款可能需要等待区块确认后才会显示为可用余额。

#### 5. 查看钱包地址

列出钱包中已使用或已生成的地址：

```bash
bitcoin-cli -rpcwallet=miner listreceivedbyaddress 0 true
```

如果只是忘记了某次生成的地址，也可以继续生成新地址。同一个钱包可以管理多个收款地址。

#### 6. 发送转账

向目标地址转账：

```bash
bitcoin-cli -rpcwallet=miner sendtoaddress <目标地址> <数量>
```

示例：

```bash
bitcoin-cli -rpcwallet=miner sendtoaddress tc1qxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx 1.5
```

发送前请确认：

- 地址是主网 `tc1q...` 地址
- 数量正确
- 钱包中有足够可用余额
- 钱包已解锁，若设置了加密密码

#### 7. 加密钱包

建议给钱包设置密码：

```bash
bitcoin-cli -rpcwallet=miner encryptwallet "你的强密码"
```

加密后，节点或钱包服务通常需要重启。以后发送交易前可能需要先解锁：

```bash
bitcoin-cli -rpcwallet=miner walletpassphrase "你的强密码" 300
```

`300` 表示解锁 300 秒。

#### 8. 备份钱包

钱包私钥保存在钱包数据中。必须备份，否则机器损坏或数据丢失后，资产无法恢复。

备份命令：

```bash
bitcoin-cli -rpcwallet=miner backupwallet /path/to/miner-wallet-backup.dat
```

Docker 容器方式示例：

```bash
docker exec -it tensorcash-wallet bitcoin-cli -datadir=/data -rpcwallet=miner backupwallet /data/miner-wallet-backup.dat
```

如果使用 Docker volume，也要备份对应的数据卷，例如 `tensorcash-data`。

#### 9. 矿池使用注意事项

矿池只需要你的收款地址，不需要私钥、钱包文件或密码。

示例：

```bash
curl -fsSL https://tensorcash.boblabs.eu/install.sh | bash -s -- --wallet tc1qxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx --label my-rig
```

安全原则：

- 不要把 `wallet.dat` 发给任何人
- 不要把私钥发给任何人
- 不要把钱包密码写进矿池脚本
- 只把公开收款地址填到矿池

#### 10. 常用命令速查

```bash
# 创建钱包
bitcoin-cli createwallet miner

# 生成收款地址
bitcoin-cli -rpcwallet=miner getnewaddress "" bech32

# 查看余额
bitcoin-cli -rpcwallet=miner getbalance

# 查看钱包信息
bitcoin-cli -rpcwallet=miner getwalletinfo

# 查看最近交易
bitcoin-cli -rpcwallet=miner listtransactions "*" 20

# 转账
bitcoin-cli -rpcwallet=miner sendtoaddress <目标地址> <数量>

# 备份钱包
bitcoin-cli -rpcwallet=miner backupwallet /path/to/backup.dat
```

#### 11. 常见问题

##### 地址不是 `tc1q...` 可以用于矿池吗？

不建议。矿池通常要求主网 TensorCash 地址，应该使用 `tc1q...` 地址。

##### 矿池需要我的私钥吗？

不需要。矿池只需要收款地址。

##### 钱包没同步完成能生成地址吗？

通常可以生成地址，但余额、交易记录和到账状态需要节点同步后才准确。

##### 钱包文件丢了怎么办？

如果没有备份，通常无法恢复。因此创建钱包后应立即备份。
